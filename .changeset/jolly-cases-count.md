---
"plumet": minor
---

Introduces global configuration support for formatting styles and a significant restructuring of `PlumetData` to enhance developer integration; the `PlumetData` interface has been evolved from a direct `PlumetCanvasMap` to a more robust structure containing `{ data?: PlumetGlobalConfig, canvas: PlumetCanvasMap }`.
